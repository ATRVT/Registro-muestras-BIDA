import { FormData } from "../types";

// Servicio de IA desactivado para esta versión.
// Se mantiene el archivo para evitar errores de importación si existen referencias residuales,
// pero simplemente devuelve los datos tal cual sin procesar.

export const cleanDataWithAI = async (data: FormData): Promise<FormData> => {
  return data;
};